
          if (millis() >= timer && hundreths >= 0) {
            hundreths--;
            timer += 100;

            countdown.setCursor(0, 0);
            countdown.print(deciSeconds);
          }
        }
      }
    }
    else {
      YOULOST();
    }
  }
